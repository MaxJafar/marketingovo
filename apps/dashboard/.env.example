# Browser requests use the versioned same-origin API by default.
# Keep this path same-origin so the HttpOnly session cookie remains protected.
# VITE_API_BASE_URL=/api/v1

# Vite proxies /api requests here during local development.
VITE_API_PROXY_TARGET=http://127.0.0.1:3210
