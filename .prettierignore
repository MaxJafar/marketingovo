node_modules/
.pnpm-store/
.turbo/
coverage/
dist/
target/
artifacts/
output/
playwright-report/
test-results/

# Generated or copied distribution inputs.
apps/desktop/src-tauri/gen/
apps/desktop/src-tauri/runtime/
apps/desktop/src-tauri/binaries/
packages/cli/dashboard/
packages/credential-broker-native/target/

# Native installer and platform resource XML is validated by its platform
# toolchain and dedicated release-policy checks; Prettier has no XML parser.
apps/desktop/src-tauri/**/*.wxs
apps/desktop/src-tauri/icons/android/**/*.xml

# Package manager and quarantined legacy snapshots are source-controlled in
# their producer's canonical format, not rewritten by Prettier.
pnpm-lock.yaml
migrations/legacy-v0/

# Generated agent-host manifests. Their exact bytes are asserted by
# `pnpm validate:plugins`, so a formatter pass here creates false drift.
plugins/claude/
.claude-plugin/
integrations/*.mcp.json

# Generated contract projections. Their exact bytes are asserted by
# `pnpm validate:contracts`, so a formatter pass here creates false drift.
gen/
packages/sdk/src/generated/

# Python intelligence-worker environment, lockfile and tool caches. Owned by
# uv, ruff and pytest in their canonical formats.
uv.lock
**/.venv/
**/.pytest_cache/
**/.ruff_cache/
.hypothesis/
