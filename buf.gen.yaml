version: v2
clean: true
plugins:
  # Go bindings for the intel daemon.
  - local: protoc-gen-go
    out: services/intel-daemon/gen/go
    opt:
      - paths=source_relative
  # Python bindings for the intelligence worker.
  - protoc_builtin: python
    out: gen/python
