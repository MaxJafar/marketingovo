# Golem Intel for OpenClaw

This adapter projects the same six workflow-level tools as MCP. It reads the
local service token at invocation time so rotation takes effect without a
gateway restart. Collection, deletion, contact reveal, outreach and governance
changes are intentionally absent.

